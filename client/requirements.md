## Packages
framer-motion | Page animations and micro-interactions
gsap | Advanced scroll animations (ScrollTrigger)
react-hook-form | Form state management
@hookform/resolvers | Zod resolver for form validation

## Notes
- Using framer-motion for hover states and UI transitions.
- Using gsap for ScrollTrigger scroll animations.
- Geist font will be loaded via Google Fonts.
- Mock data is used for the majority of the content, but the contact form is wired up using react-hook-form and the API hook as requested.
